# platform-specific
.DS_Store
Thumbs.db
desktop.ini

# python / tooling
__pycache__/
*.py[cod]
*.egg-info/
.venv/
# docs build output (task 28) — built, never committed
docs/_build/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage*
# uv
uv.lock.bak
# data & caches (never in git — AGENTS.md)
validation/data/cache/
# icon4py datatest cache if ICON4PY_TEST_DATA_PATH is pointed at the repo (the
# symcon.icon.testing bridge defaults it to ~/.cache/symcon/icon4py-testdata)
testdata/
*.nc
*.grb
*.grb2
*.zarr/
# plan artifacts & agent-local files
development/work/reports/report-0004-coupling-algebra/
.claude/settings.local.json
# local reference documents
development/references/local/*
!development/references/local/README.md
# native-driver build cache never lands in-repo, but belt and braces
*.sdfg
*.so

# L8 gradient-decay plots (S10) are artifacts, never data in git
validation/L8_gradients/artifacts/

# L4 JW trajectory artifacts (S13): plots/norm tables, never data in git
validation/L4_idealized/artifacts/
*.npz
