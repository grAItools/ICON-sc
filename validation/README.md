# validation/

The architecture §9 validation ladder as a runnable tree (integration tier), one
directory per level (L2–L8). `data/` holds pooch registries and fetch tooling only —
reference data never lands in git (AGENTS.md).
