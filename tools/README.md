# tools/

Operational scripts: plan_inspect, grid_partition, names_audit, constraints_update
(see repo layout doc §4). `constraints_update.py` regenerates constraints/ lockfiles
against fresh gt4py/icon4py — pin changes remain trunk decisions.
