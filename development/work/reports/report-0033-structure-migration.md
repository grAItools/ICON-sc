# Task 33 — structure migration (plan/ → development/): execution report

**Branch:** `task/33-structure-migration` · **Date:** 2026-07-14 · **State:** executed,
all gates green (dated outputs in §4)

Plan: `development/work/plans/plan-0033-structure-migration.md` (frozen at assignment). Commits:
C1 `0e54c42` (moves) · C2 `2a1943d` (retargets) · C3 `69d0384` (governance + skeleton)
· C4 `41ee2c0` (living rewrites) · C5 `27e1fff` (code/config/tooling + diff artifact)
· C6 = this report.

## 1. Move ledger (plan §4)

Every row of the §4 move map executed as `git mv` in C1; nothing else in the commit.
Purity: `git show --summary -M100 0e54c42 | grep -cE "^ rename"` → **71**;
`git show -M100 --name-status --format="" 0e54c42 | grep -vcE "^R100"` → **0**
(all pure R100 renames, no `M`/`A`/`D`).

Deviations:

- **Rename count 71, not the plan's stated 66.** The plan's count stops at move-map
  row 13 (4 + 42 + 6 + 1 + 8 + 1 + 1 + 3 = 66) and omits rows 14–18 (the three
  report subdirectories' files, the reports README, and `references/README.md` = 5
  more). All 18 rows were executed; 71 = every tracked file under `plan/` plus
  `references/README.md`. No file was skipped and none added.
- The purity command as written in plan §4 (`git show --stat -M100 … | grep -cE
  "^ rename"`) counts 0 because `--stat` does not emit `rename` lines; the
  equivalent `--summary` form (above) was used.

## 2. Retarget statistics (plan §5.1, commit C2)

21 files changed, 242 replacements (insertions = deletions = 242; word-diff added
fragments are path strings only). Replacement targets by destination: records/ 121,
plans/ 61, archive/ 26, DECISIONS.md 20, ideas/ 15, specs/ 11, policies/ 2, plus 13
multi-kind/brace-token rewrites (below).

Rules applied beyond the deterministic table rows:

- **Multi-kind generic `plan/steps/` mentions** (the table's "pick per what the
  sentence refers to" row): where a sentence refers to the SPEC/PLAN/STATUS triad as
  a whole, the path token was rewritten as `development/{specs,plans,records}/…`
  (7 instances, in `archive/plan-tree-map.md` and the 29 report); the task-30
  prompt's exemplar folders became `development/{specs,plans}/SXX_….md` (3) and its
  "no file outside" fence `development/{specs,plans}/<new files>` (1).
- **Quoted config values** inside frozen reports (the settings.json deny glob, the
  `.gitignore` artifacts line, the PR-template line quoted in the 29/32 reports) were
  rewritten to their post-C5 values so the quotes match the repo again.
- `outlines/` (bare, in `records/00_OVERVIEW.md`) → `development/ideas/`; the task-20
  prompt's "(create `reports/`)" → "(create `development/records/`)".

Left alone (by design / ambiguous), each still matching the §7.1 pattern:

- `development/plans/33_structure_migration.md` — the migration plan itself
  (47 pattern hits): its move map "From" column, §5.1 "Old string" column, §7.1 grep
  pattern, and §5 instructions must name the old tree; retargeting would destroy the
  plan's meaning. Left byte-identical to the assigned prompt.
- `development/records/31_plan_structure_migration_REPORT.md:76` — "all
  `plan/steps/*` and prompt files 10–30 byte-identical, no file moved or renamed" is
  task-31's verification claim *about the old paths themselves*; retargeting would
  falsify the record.
- Bare `plan/` (tree) mentions inside frozen records (29/32 reports, archived
  plan-tree map §4) that describe the old tree as historical subject matter — they do
  not match the §7.1 pattern and were left as history.
- `references/` prose mentions inside the 32 report's collision analysis (§2.7) —
  historical subject matter, left.
- Bare `TRUNK_DECISIONS.md` / `10_REVIEW_PROTOCOL.md` filename mentions (no `plan/`
  prefix) in frozen records and prompts — historical namings, left (see §4 gate 7.1b).

## 3. Untracked files relocated (plain `mv`, never `git add`ed)

| From | To |
|---|---|
| `references/icon-model-tutorial-2025.pdf` | `development/references/local/icon-model-tutorial-2025.pdf` |
| `references/phd_thesis_ubbiali.pdf` | `development/references/local/phd_thesis_ubbiali.pdf` |
| `references/sympl-climt.pdf` | `development/references/local/sympl-climt.pdf` |
| `references/prompts-backups.md` | `development/references/local/prompts-backups.md` |
| `plan/outlines/prompts-backups.md` | `development/references/local/prompts-backups-outlines.md` (**renamed**: name collision with the file above; the outlines copy is a strict prefix — lines 1–6 — of the references copy, both kept) |
| `plan/steps/S04_coupling_algebra/artifacts/convergence_ode.png` | `development/records/S04_coupling_algebra/artifacts/convergence_ode.png` |
| `plan/steps/S04_coupling_algebra/artifacts/convergence_burgers.png` | `development/records/S04_coupling_algebra/artifacts/convergence_burgers.png` |

(The S04 PNGs are regenerated by the fast gate:
`uv run pytest packages/symcon-core/tests/test_order_ode.py packages/symcon-core/tests/test_order_burgers.py -q`.)

## 4. Gate outputs (verbatim summary lines, dated)

All commands run from the repo root on `task/33-structure-migration` after C5.

- **§7.1a residual-path grep** (2026-07-14 01:28 CEST) — hits, all accounted:
  `development/references/lock.toml:471` (untouchable append-only ledger, hard rule);
  `development/DECISIONS.md:12` (the mandated "Formerly plan/TRUNK_DECISIONS.md"
  line); `development/adr/0001…:27` and `development/adr/0003…:8` (ADR prose naming
  the old file historically); `development/records/31_plan_structure_migration_REPORT.md:76`
  (§2 above); `development/plans/33_structure_migration.md` (47 hits, §2 above); and
  this report's own quotations of old paths (self-referential, like the plan's).
  *(Correction, review round 1: an earlier revision of this line also listed the two
  untracked `prompts-backups*.md` files as hits — they contain zero pattern matches.)*
  **Deviation from the plan's expectation:** the plan predicts
  exactly one hit at `docs/architecture/symcon_repo_layout.md:104`; that line is the
  `symcon.core.plan` source-package node (never-touch) and does not match the grep
  pattern — the layout doc contributes zero hits (see the note inside
  `layout-doc-revision.diff`).
- **§7.1b `grep -rn "TRUNK_DECISIONS" . --exclude-dir=.git`** (2026-07-14 01:28
  CEST): hits only in `development/DECISIONS.md:12` (formerly-line), ADRs 0001/0003,
  the archived plan-tree map, the 33 plan, and record prose in the 29/31/32 reports —
  all historical namings; nothing else in the repo.
- **§7.2 relative-link check** (2026-07-14 01:28 CEST): zero `BROKEN` lines, exit 0.
  No exceptions needed; no links to gitignored artifacts found.
- **§7.3 TD-PENDING audit** (2026-07-14 01:28 CEST): zero live sign-off flags (as
  before the migration — task 31 recorded "zero live markers"). Every
  `grep -rn "TD-PENDING" development/` hit is convention prose naming the marker
  (policies, ADR-0003, the 29/31/32 records, the archived map, the 33 plan), not a
  flag; no hit lacks its register correspondence.
- **§7.4 pytest battery** (baselines: `development/policies/verification-gates.md`):
  - fast (`not gpu and not slow`), 2026-07-14: `739 passed, 1 skipped, 143 deselected, 51 warnings in 848.87s (0:14:08)` (01:43 CEST; the skip is the mpi opt-in; S04 order tests wrote `development/records/S04_coupling_algebra/artifacts/convergence_{ode,burgers}.png` at the new path)
  - slow-no-data, 2026-07-14: `31 passed, 852 deselected, 5 warnings in 772.42s (0:12:52)` (01:58 CEST)
  - data-not-slow, 2026-07-14: `43 passed, 840 deselected, 11 warnings in 611.21s (0:10:11)` (02:08 CEST)
  - data-slow, 2026-07-14: `76 passed, 1 skipped, 806 deselected, 28 warnings in 3313.12s (0:55:13)` (03:04 CEST; count at baseline incl. the upstream MCH-only diffusion skip; wall time above the ~32 min reference — loaded host, counts are the contract)
- **ruff check** (2026-07-14 01:28 CEST): `All checks passed!`
- **ruff format --check** (2026-07-14 01:28 CEST): `173 files already formatted`
- **mypy --strict -p symcon.core** (2026-07-14 01:29 CEST):
  `Success: no issues found in 50 source files`
- **lint-imports** (2026-07-14 01:29 CEST): `Contracts: 2 kept, 0 broken.`
- **§7.5 Sphinx** (2026-07-14 01:29 CEST):
  `uv run sphinx-build -E -W --keep-going -b html docs docs/_build/html` → exit 0,
  `build succeeded.`
- **§7.6 tree checks** (2026-07-14 01:28 CEST; corrected in review round 1):
  `git ls-files plan/ references/` → empty; `ls development/` = `DECISIONS.md
  README.md adr archive ideas plans policies records references specs` (exactly the
  §1 tree). *(Correction: the original revision of this line claimed the `plan/` and
  `references/` directories were gone — false when written: empty, untracked
  directory husks remained after the untracked-file relocations (review MAJOR-1).
  Removed via `find plan references -depth -type d -empty -delete` in round 1;
  `[ ! -d plan ]` and `[ ! -d references ]` now pass, re-checked 2026-07-14.)*
- **Untouchables:** `git diff main -- docs/architecture REFERENCES.lock constraints/
  uv.lock` → empty (2026-07-14 01:33 CEST).

## 5. Register rows and status flips (plan §3.3, commit C4)

Added (all `pending`, Date `(merge)`): **TD-33.1** (tree reorganization; supersedes
TD-29.1 and TD-29.6), **TD-33.2** (content-frozen amendment, ADR-0002), **TD-33.3**
(register rename + ledger/ADR no-merge, ADR-0003), **TD-33.4** (layout-doc revision
diff artifact; marks the re-draft of TD-29.7). Flipped: TD-29.1 `signed-off` →
`superseded(TD-33.1)`; TD-29.6 `pending (…)` → `superseded(TD-33.1)`; TD-29.7
`pending (…)` → `superseded(TD-33.4)`. No other cells of those rows touched.
Task-register (`development/plans/README.md`): row 33 → `executed`, deliverables of
rows 32/33 per plan §5.5; deliverable paths of all rows retargeted to `records/`.

## 6. Diff artifact and by-design gate hits

`development/work/reports/report-0033-structure-migration/layout-doc-revision.diff` — verified with
`git apply --check` (applies cleanly). **Discrepancy vs plan §5.13** (recorded in the
artifact header): the current layout doc has no `plan/` node in its §4 repo tree
(line 104 is the `symcon.core.plan` package node) and no top-level `references/`
node, so the diff *adds* the `development/` node (one line per folder) before the
`docs/` node instead of replacing/deleting. By-design §7.1/§7.2 leftovers: listed in
§2 and §4 above; §7.2 has none.

## 7. Deviations (consolidated) and follow-ups

Deviations from the plan text, all mechanical-and-obvious per the plan's preamble:

1. Move count 71 vs stated 66 (§1); purity command needed `--summary` (§1).
2. `plan/outlines/prompts-backups.md` renamed on relocation to avoid a collision (§3).
3. The migration plan itself and the task-31 no-move claim were exempted from C2
   retargeting (§2) — sanctioned by plan §8(6) "hits left by design".
4. §7.1's "exactly one hit at symcon_repo_layout.md:104" expectation is unsatisfiable
   (§4, §6); the actual by-design hit set is documented in §4.
5. §5.13's "replace the plan/ node" became "add a development/ node" (§6).
6. C3 purity ("only `A` lines + the two header-prepend `M`s"): the sanctioned
   prepends touch 7 files (6 outlines + archive map), plus the §5.3-mandated
   first-line rename of `development/references/local/README.md` — 8 `M` lines, 24
   `A` lines; no other modifications.
7. Root README (§5.8): the stale top-level `references/` row was folded into the new
   `development/references/` row rather than kept pointing at a deleted directory.
8. Post-gate polish folded into C6: `development/policies/review-protocol.md` step 1
   now names the step-PLAN home explicitly and step 2 points at
   `development/policies/verification-gates.md` (the baselines moved out of
   plans/README.md in C4).
9. §5.11 line re-wrap: the retargeted artifact path in `test_order_ode.py:101` /
   `test_order_burgers.py:87` exceeds the 100-column limit on one line; wrapped in
   parentheses exactly as `ruff format` produces (`parents[3]` depth unchanged;
   verified at runtime by the fast gate writing the PNGs).

Follow-ups:

- Owner: apply or reject `layout-doc-revision.diff` (TD-33.4); sign off
  TD-33.1–TD-33.4 and fill their Date cells at merge, plus the ADR 0001–0003
  `Date:` fields.
- The pending prompts 20–25/30 retain historical "Review checklist (appended to
  10_REVIEW_PROTOCOL.md …)" headings; the protocol now lives at
  `development/policies/review-protocol.md` (register row 10 says so) — future
  prompt authors should cite the new path.
- `development/references/local/prompts-backups.md` and `…-outlines.md` are untracked
  owner notes relocated as-is; the owner may want to consolidate them (the outlines
  copy is a strict prefix of the other).

## 8. Review fixes (round 1)

Reviewer verdict: request-changes (1 MAJOR, 2 MINOR, 4 INFO). Fixes applied:

1. **MAJOR-1** — empty, untracked `plan/`/`references/` directory husks survived the
   migration (git tracks no directories; the untracked-file `mv`s left the skeletons),
   so plan §7.6's `[ ! -d plan ]` failed; §4's original "directories gone" line was
   written without observing that check. Fixed: husks deleted, §7.6 re-run green,
   §4 line corrected in place with the correction marked. Root cause of the false
   line: the report's gate section was drafted from the implementer's C5-time
   worklog and finalized across the implementer→orchestrator session handoff without
   re-observing the §7.6 predicate.
2. **MINOR-2** — §4's §7.1a hit list named the two untracked `prompts-backups*.md`
   files as residual-grep hits; they contain zero matches. Corrected in place;
   the report's own self-referential quotations added to the accounted set.
3. **MINOR-3** — C2 had retargeted the Decision-text cell of the superseded TD-29.1
   row, making the historical zero-move decision read as if it had ratified
   `development/records/`. Restored the original wording (`plan/prompts/reports/`),
   Status flip untouched — the ADR-0002 exemption class the implementer already
   applied to the task-31 report. Neighboring rows checked: TD-29.2 "(this file)" and
   TD-29.3's pointer stay retargeted (their subjects survive under new names — the
   pointers remain true); TD-29.6's `plan/drafts/` was never retargeted (correct).
4. INFO-4/5/6/7 — accepted as noted, no changes: the C6 review-protocol polish stands
   as declared (deviation 8); `document-kinds.md`'s "frozen at step start"
   wording kept (equivalent in effect to §3.2's "at acceptance/assignment");
   the two by-design historical mentions (20_gpu_validation.md:50, archived map:55)
   left; the plan-internal defects list already covered by §7.
