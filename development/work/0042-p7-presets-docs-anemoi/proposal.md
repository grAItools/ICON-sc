**Status:** accepted-roadmap (graduates to development/work/specs/ via plan 0030).

# P7 — Presets, docs, anemoi, release hygiene (outline)
**After:** P3/P4 mature
Steps: validated/experimental preset registry + the coupling-experiment catalogue (SUS↔STS↔SSUS on the real model — the §10 headline study, executed); L7 full self-convergence on idealized cases incl. the satad first-order caveat reproduction; anemoi-layout ZarrMonitor + `PrognosticState↔anemoi.State` adapters; docs build (architecture doc canonicalized, API autodoc, porting guide from the S07/S08/P3 pattern); versioning + release automation; `tools/constraints_update.py` scheduled job.
