"""symcon.icon — the ICON model as a client of symcon.core.

Grid stack, ICON components, presets, ingestion, and the variable registry arrive in
S06+. May import ``symcon.core`` and icon4py; ``symcon.core`` must never import this.
"""

__version__ = "0.1.0"
